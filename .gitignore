/dist/
/bin/
*.test
*.out

# Never commit identities or state
*.key
*.identity
/state/
/var/
identity.json
gateway-state.json

# Copied from the repository root at deploy time so the published script and
# the readable one cannot drift apart.
/site/install.sh
